/**
 * Демонстрационный тест для проверки компактного вывода
 * 
 * Этот файл создан для демонстрации работы компактного вывода тестов.
 * В компактном режиме (npm test) будут показаны только упавшие тесты.
 * В verbose режиме (npm run test:verbose) будут показаны все тесты.
 */

describe('Демонстрация компактного вывода', () => {
  describe('Успешные тесты', () => {
    it('должен пройти тест 1', () => {
      expect(1 + 1).toBe(2);
    });

    it('должен пройти тест 2', () => {
      expect('hello').toBe('hello');
    });

    it('должен пройти тест 3', () => {
      expect([1, 2, 3]).toHaveLength(3);
    });

    it('должен пройти тест 4', () => {
      expect({ a: 1, b: 2 }).toEqual({ a: 1, b: 2 });
    });

    it('должен пройти тест 5', () => {
      expect(true).toBeTruthy();
    });
  });

  describe('Упавший тест (для демонстрации)', () => {
    // Этот тест намеренно пропущен, чтобы не ломать CI
    it.skip('должен показать детали при падении', () => {
      // Раскомментируйте следующую строку, чтобы увидеть детали упавшего теста
      // expect(1 + 1).toBe(3);
    });
  });
});
